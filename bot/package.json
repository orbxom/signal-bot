{
  "name": "signal-family-bot",
  "version": "1.0.0",
  "description": "Claude-powered Signal bot for family chat",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "dev:test": "SIGNAL_CLI_URL=${SIGNAL_CLI_URL:-http://localhost:9090} tsx watch src/index.ts --test-channel-only",
    "start": "node dist/index.js",
    "test": "vitest",
    "test:integration": "vitest run tests/integration.test.ts",
    "lint": "biome lint ./src ./tests",
    "lint:fix": "biome lint --write ./src ./tests",
    "format": "biome format --write ./src ./tests",
    "format:check": "biome format ./src ./tests",
    "check": "biome check ./src ./tests",
    "check:fix": "biome check --write ./src ./tests",
    "mock-signal": "tsx src/mock/signalServer.ts"
  },
  "keywords": [
    "signal",
    "bot",
    "claude",
    "anthropic"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@playwright/mcp": "^0.0.68",
    "better-sqlite3": "^11.0.0",
    "croner": "^10.0.1",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
