---
name: run-bot
description: Use when the user wants to start the bot for all channels, run dev mode unrestricted, or says "run the bot on all groups" or "start the bot for everything".
---

# Run Bot (All Channels)

Starts the bot connected to real signal-cli, processing all group chats.

```bash
cd /home/zknowles/personal/signal-bot/bot
npm run dev
```

Run this as a background task. Wait for `Starting message polling...` in the output before considering it ready.
